# Example only. The current vless-node entrypoint does not load this file.
# Runtime settings come from CLI/env flags and UniProxy panel data.
# See README.md for the supported environment variables and reload behavior.
